<config xmlns="urn:org:jgroups"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="urn:org:jgroups http://www.jgroups.org/schema/JGroups-3.4.xsd">
    <TCP_NIO2 bind_addr="${jgroups.tcp.bind_addr}"
		 bind_port="${jgroups.tcp.bind_port}"
		 port_range="0"
         recv_buf_size="${tcp.recv_buf_size:5M}"
         send_buf_size="${tcp.send_buf_size:5M}"
         max_bundle_size="4k"
         max_bundle_timeout="1"
         max_read_batch_size="${max.read.batch.size:10}"
         sock_conn_timeout="300"

         timer_type="new3"
         timer.min_threads="4"
         timer.max_threads="10"
         timer.keep_alive_time="3000"
         timer.queue_max_size="3000"

         thread_pool.enabled="true"
         thread_pool.min_threads="${jgroups.threads.min:5}"
         thread_pool.max_threads="${jgroups.threads.max:60}"
         thread_pool.keep_alive_time="5000"
         thread_pool.queue_enabled="${jgroups.threads.queue:false}"
         thread_pool.queue_max_size="${jgroups.threads.queue.size:500000}"
         thread_pool.rejection_policy="Run"

         oob_thread_pool.enabled="true"
         oob_thread_pool.min_threads="5"
         oob_thread_pool.max_threads="15"
         oob_thread_pool.keep_alive_time="50000"
         oob_thread_pool.queue_enabled="true"
         oob_thread_pool.queue_max_size="10000"
         oob_thread_pool.rejection_policy="Run"/>
    <TCPPING timeout="${nodes.ping.timeout:2000}" initial_hosts="${jgroups.tcpping.initial_hosts}" port_range="0" />
    <MERGE3  min_interval="1000"
             max_interval="3000"/>
    <FD_SOCK/>
    <VERIFY_SUSPECT timeout="1500"  />
    <BARRIER />
    <pbcast.NAKACK2 use_mcast_xmit="false"
                    discard_delivered_msgs="true"/>
    <UNICAST3 xmit_interval="${retransmit.interval:100}"
              max_retransmit_time="${max.retransmit.time:150}"
              />
    <SCOPE />
    <pbcast.STABLE stability_delay="1000" desired_avg_gossip="50000"
                   max_bytes="4M"/>
    <pbcast.GMS print_local_addr="true" join_timeout="3000"
                view_bundling="true"/>
    <pbcast.STATE_TRANSFER/>
</config>